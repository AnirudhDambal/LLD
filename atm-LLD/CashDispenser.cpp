#include "include/NoteDispenser.h"
#include "include/DispenseChain.h"

// Alternate helpers are implemented in the hyphenated file (to keep compatibility with the
// repository's original naming). This file intentionally does not define any global
// functions to avoid duplicate symbols when both files are compiled.
namespace atm_impl {
    // helper utilities (if needed) could go here
}
